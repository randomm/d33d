"""Eval harness package (issue #9).

Contains the deterministic gate implementations (gates.py, task-gates),
the failures.jsonl pin (failure_capture.py, task-failures), the gate 6
slice dry-run delegation (slice_gate.py, task-slice), gate 7 region
containment (region_gate.py, task-region), the case schema
(case_schema.py, task-golden-set), and the promptfoo harness + report
+ judge (harness.py, report.py, judge.py, task-harness).
"""
